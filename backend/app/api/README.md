# Rotas e endpoints do backend

Coloque aqui os arquivos de rotas/endpoints da API FastAPI.

Exemplo:
```python
# backend/app/api/user.py
from fastapi import APIRouter
router = APIRouter()

@router.get("/users")
def get_users():
    return []
```
